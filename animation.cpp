    else if (compteurTps%9 == 3)
    {
        gl.texture(); // Applique la texture à l'état 4.
    }
    else if (compteurTps%9 == 4)
    {
        gl.texture(); // Applique la texture à l'état 5.
    }
    else if (compteurTps%9 == 5)
    {
        gl.texture(); // Applique la texture à l'état 6.
    }
    else if (compteurTps%9 == 6)
    {
        gl.texture(); // Applique la texture à l'état 6.
    }
    else if (compteurTps%9 == 7)
    {
        gl.texture(); // Applique la texture à l'état 6.
    }
    else if (compteurTps%9 == 8)
    {
        gl.texture(); // Applique la texture à l'état 6.
    }